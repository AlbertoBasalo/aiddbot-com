---
import { getEntry } from "astro:content";
import DocPage from "@layouts/DocPage.astro";

const entry = await getEntry("docs", "workflow");
if (!entry) {
	return Astro.redirect("/404");
}
---

<DocPage entry={entry} />
